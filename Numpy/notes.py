# a.ndim, a.shape is used to find the dimension or shape of the array in the numpy 
# np.zeros(2) np.ones() np.empty() np.arrange() 
# array([0., 0.])
# np.sort(arr) to sor the array
# np.concatenate((arr1, arr2), axis=0) to concatenate the array
# np.array_split(arr, 3) to split the array into 3 parts
# ndarray.ndim, ndarray.shape, ndarray.size, ndarray.dtype, ndarray.itemsize, ndarray.data
# b = a.reshape(3, 2) to reshape the array in 3 rows and 2 columns
# print(a[a < 5]) to print the elements less than 5
# np.vstack((a1, a2)) to stack the arrays vertically , np.hstack((a1, a2)) to stack the arrays horizontally
# a.sum(), a.min(), a.max() to find the sum, min, max of the array
# a.sum(axis=0) to find the sum of the array along the column
# np.sqrt(a) to find the square root of the array
# np.std(a) to find the standard deviation of the array
# np.unique(a) to find the unique elements in the array
# np.arange(6).reshape((2, 3)) to create an array of 2 rows and 3 columns
# np.flip(arr) to flip the array ( reverse the array  )